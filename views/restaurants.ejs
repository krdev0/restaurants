<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('parts/head') %>
    <link rel="stylesheet" href="styles/restaurants.css">
</head>

<body>
    <%- include('parts/header') %>
        <%- include('parts/aside') %>
            <main>
                <h1>Recommended restaurants</h1>
                <% if(numberOfRestaurants> 0) { %>
                    <p>Find your next favorite restaurants with help of our other users!</p>
                    <p>We found <%= numberOfRestaurants %> restaurants</p>
                    <ul id="restaurants-list">
                        <% for(const restaurant of restaurants) { %>
                            <li class="restaurant-item">
                                <article>
                                    <h2>
                                        <%= restaurant.name %>
                                    </h2>
                                    <div class="restaurant-meta">
                                        <p>
                                            <%= restaurant.cuisine %>
                                        </p>
                                        <p>
                                            <%= restaurant.address %>
                                        </p>
                                    </div>
                                    <p>
                                        <%= restaurant.description %>
                                    </p>
                                    <div class="restaurant-actions">
                                        <a href="<%= restaurant.website %>">View Website</a>
                                    </div>
                                </article>
                            </li>
                            <% } %>
                    </ul>
                    <% } else { %>
                        <p>We have no restaurant reviews yet, maybe start recommending some!</p>
                        <% } %>
            </main>
</body>

</html>